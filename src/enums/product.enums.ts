// --- PRODUCT CATEGORY  ---
export enum ProductCategory {
  DEFAULT = 0,
  WOMEN = 1,
  MEN = 2,
  KIDS = 3,
}

// --- PRODUCT TYPE  ---
export enum ProductType {
  DEFAULT = 0,
  ACCESSORIES = 1,
  CLOTHING = 2,
  SHOES = 3,
}

// --- PRODUCT SUB_TYPE  ---
export enum ProductSubType {
  DEFAULT = 0,
  T_SHIRT = 1,
  JEANS = 2,
  BLAZERS = 3,
  JACKETS = 4,
  SHIRTS = 5,
  SKIRTS = 6,
  TROUSERS = 7,
  TOPS = 8,
  DRESSES = 9,
  BAGS = 10,
  JEWELRY = 11,
}

// --- SIZES (XS, S, M...) ---
export enum ProductSize {
  DEFAULT = 0,
  XXS = 1,
  XS = 2,
  S = 3,
  M = 4,
  L = 5,
  XL = 6,
  XXL = 7,
}
